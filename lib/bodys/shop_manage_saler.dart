import 'package:flutter/material.dart';

class ShopManageSaler extends StatelessWidget {
  const ShopManageSaler({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is Shop Manage'),
    );
  }
}