import 'package:flutter/material.dart';

class ShowOrderSaler extends StatelessWidget {
  const ShowOrderSaler({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is Show order'),
    );
  }
}